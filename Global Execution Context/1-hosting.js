x = 30;

console.log(`Value of X is ${x}`);

var x = 10;

console.log(`Value of X is ${x}`);

// in case of var, the variable is hoisted to the top

// but in let and const the variable is not hoisted to the top

// but let and const are also hoisted to the top but in a different way (temporal dead zone)
