// In JS everything is an object, even functions

let fname = "Tanish Singhal";

console.log(fname.length);  // if fname is a string, then how can i able to use . operator in it.
// This is because of wraper Classes

const lname = new String("Singhal");

console.log(lname);